<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add</title>
    <style>
        body{
            background-color: lightcyan;
            margin: 0;
        }
        form{
            display: flex;
            flex-direction: column;
            width: 50%;
            border-radius: 10px;
            border: 1px solid white;
            padding: 50px;
            margin: 0 auto;
            gap: 10px;
            background: linear-gradient(rgb(158, 228, 158),rgb(42, 133, 42));
        }
        input{
            padding:15px;
            margin: 20px;
            border-radius: 10px;
            border: white solid 1px;
        }
        button{
            width: 40%;
            padding: 10px;
            border-radius: 10px;
            border: 1px solid rgb(4, 4, 240);
            background-color: rgb(4, 4, 240);
            color: white;
            font-weight: 800;
        }
        .btn{
            display: flex;
            justify-content: center;
        }
        h2{
            font-weight: 800;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }
    </style>
</head>
<body>
    <%- include('./partials/headers.ejs'),{mycookie} %>
    <form action="/post-signup" method="post" onsubmit="">
        <h2>Sig up</h2>
        <input  type="text" placeholder="username" name="username"> 
        <input  type="password" placeholder="password" name="password">
        <div class="btn">
            <button type="submit" >Register</button>
        </div>
    </form>
</body>
</html>

